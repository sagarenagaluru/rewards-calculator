spring:
  jpa:
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb


server:
  port: 8123

logging:
  level:
    com.rewards.rewardscalculator: INFO
